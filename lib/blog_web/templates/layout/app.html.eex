<!DOCTYPE html>
<html lang="fa" dir="rtl">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Blog · Phoenix Framework</title>
    <link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazir-font@v27.1.0/dist/font-face.css" rel="stylesheet" type="text/css" />
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <style>
    body {
      font-family: 'Vazir';
    }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light fixed-top">
      <div class="container-fluid">
        <%= link "خانه", to: Routes.post_path(@conn, :index), class: "navbar-brand" %>
        <div class="collapse d-flex">
          <div class="navbar-nav">
            <%= if Pow.Plug.current_user(@conn) do %>
              <%= link "داشبورد", to: Routes.admin_path(@conn, :show), class: "nav-link" %>
              <%= link "مقاله‌ی جدید", to: Routes.post_path(@conn, :new), class: "nav-link" %>
              <%= link "خروج", to: Routes.session_path(@conn, :delete), method: :delete, class: "btn btn-danger" %>
            <% else %>
              <%= link "ورود", to: Routes.session_path(@conn, :new), class: "btn btn-primary" %>
            <% end %>
            <%= if function_exported?(Routes, :live_dashboard_path, 2) do %>
              <%= link "LiveDashboard", to: Routes.live_dashboard_path(@conn, :home), class: "nav-link" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </div>

  </body>
</html>
